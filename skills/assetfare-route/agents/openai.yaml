interface:
  display_name: "AssetFare Route"
  short_description: "Verify and use capped non-custodial AssetFare routes."
  default_prompt: "Use $assetfare-route to compare the requested supported route across Solana, Base, Arbitrum, Robinhood Chain, or Polygon native-USDC source routes. Verify live capabilities and the signed manifest, obtain a fresh REST quote, and stop before authentication, preparation, signing, or submission unless the caller explicitly approves continuation."
