{
  "name": "@assetfare/agenti-route-tools",
  "version": "0.1.1",
  "description": "Read-only AssetFare route tools that compose with Agenti",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/index.test.js"
  },
  "peerDependencies": {
    "ai": ">=4 <5",
    "zod": ">=3.23 <4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ai": "7.0.107",
    "typescript": "^5.7.0",
    "zod": "3.25.76"
  },
  "engines": {"node": ">=20"},
  "license": "MIT"
}
