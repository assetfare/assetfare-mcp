{
  "name": "@assetfare/agenti-route-tools",
  "version": "0.2.0",
  "description": "Read-only AssetFare route tools with a fail-closed, verified direct provider path for Agenti",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/index.test.js"
  },
  "peerDependencies": {
    "ai": ">=5 <6",
    "zod": ">=3.23 <4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ai": "5.0.207",
    "typescript": "^5.7.0",
    "zod": "3.25.76"
  },
  "overrides": {
    "jsondiffpatch": "0.7.6"
  },
  "engines": {"node": ">=20"},
  "license": "MIT"
}
